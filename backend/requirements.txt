# AI Chat Agent Backend — FastAPI + SQLAlchemy + Firebase + MCP
# Fix versions for reproducible deploys
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
aiosqlite==0.20.0

# Firebase
firebase-admin==6.6.0
PyJWT==2.10.0

# HTTP Client + Retry
httpx[http2]==0.27.2
tenacity==9.0.0

# MCP SDK
mcp==1.0.0

# Search
# tavily-python is optional; we use direct httpx to Tavily API

# Rate Limiting
slowapi==0.1.9

# Validation
pydantic==2.9.2
pydantic-settings==2.6.1

# CORS / Middleware
python-multipart==0.0.17
starlette==0.41.0

# JSON serialization for SSE
orjson==3.10.12
